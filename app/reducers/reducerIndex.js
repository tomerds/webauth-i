import { SUBMIT_LOGIN_START } from '../actions';


initialState = {

};

function reducer(state = initialState, action) {
  switch (action.type) {
    case SUBMIT_LOGIN_START:
      return {
        ...state,

      }
  }
}